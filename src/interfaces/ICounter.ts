export default interface ICounter {
  id: string;
  dateID: number;
  title: string;
  description: string;
  counter: number;
  goal: number;
  defaultInput: number;
  color: string;
  textColor: string;
}
